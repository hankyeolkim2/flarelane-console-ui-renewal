# FlareLane 콘솔 UI 리뉴얼 데모 — Claude Code 작업 지시서

이 문서를 Claude Code에 그대로 붙여넣고 작업을 시작하세요.
순서대로 진행하는 것을 권장합니다. (0번 세팅 → 1번 토큰/레이아웃 → 2번부터 페이지)

---

## 프로젝트 개요

FlareLane(B2B 고객 메시징 SaaS)의 관리자 콘솔 UI를 현대화하는 **데모/프로토타입**입니다.
의사결정자에게 "구조·기능은 그대로 두고 UI만 현대화하면 이렇게 된다"를 보여주는 것이 목적입니다.

**핵심 원칙**
- 기존 콘솔의 레이아웃과 정보 구조는 **그대로 유지**한다. 재배치하거나 기능을 새로 만들지 않는다.
- 바꾸는 것은 오직 **시각적 톤**(색, 타이포, 간격, 모서리, 컴포넌트 스타일)이다.
- 실제 데이터·API 연동은 없다. 모두 **더미 데이터**로 채운다.
- 모든 화면은 같은 컴포넌트(테이블, 카드, 버튼, 배지 등)를 재사용해 일관성을 보여준다.

---

## 0. 프로젝트 세팅

다음 스택으로 새 프로젝트를 생성하세요.

- Next.js (App Router) + TypeScript
- Tailwind CSS
- shadcn/ui
- 차트: recharts (shadcn chart 컴포넌트 사용)
- 테이블: TanStack Table (정렬/필터/페이지네이션 로직)
- 폰트: Pretendard (한국어 UI)
- 아이콘: lucide-react

세팅 명령 예시:
```
npx create-next-app@latest flarelane-console-demo --typescript --tailwind --app
cd flarelane-console-demo
npx shadcn@latest init
```

필요한 shadcn 컴포넌트는 작업하며 추가하세요. (예: button, card, table, badge, input, select, dropdown-menu, dialog, tabs, separator 등)

---

## 1. 디자인 토큰 (가장 먼저 globals.css / tailwind 설정에 등록)

### 브랜드 네이비 (액션·강조용)
```
navy-50:  #EAEEF5
navy-100: #C7D3E6
navy-200: #9AAFD0
navy-400: #4C6699
navy-600: #284782
navy-700: #163966   (버튼 hover)
navy-800: #002952   (기본 버튼, 주요 강조 — 메인 브랜드 색)
navy-900: #001733   (사이드바 배경, 가장 진한 텍스트)
```

### 그레이 (텍스트·배경·보더용)
```
gray-50:  #FCFCFD   (페이지 배경)
gray-100: #F7F7FA   (카드/표면 배경)
gray-200: #E4E4EA   (기본 보더)
gray-300: #C8C8D0   (hover 보더)
gray-400: #A0A0AA   (비활성 텍스트/아이콘)
gray-500: #7A7A83   (힌트 텍스트)
gray-600: #5E5E65   (보조 텍스트)
gray-700: #44444A   (본문 텍스트)
gray-800: #2D2D31   (강조 텍스트)
gray-900: #1B1B1E   (제목 텍스트)
gray-950: #0B0B0C   (최진 텍스트)
```

### 상태색 (배지·알림용, 표준 톤 사용)
- 성공/활성: 초록 계열
- 경고: 노랑/앰버 계열
- 위험/삭제: 빨강 계열

### 시맨틱 토큰 매핑 (CSS 변수로 연결)
```
--color-brand:         navy-800
--color-brand-hover:   navy-700
--color-brand-subtle:  navy-50    (선택된 행, 연한 강조 배경)
--color-sidebar-bg:    navy-900
--color-page-bg:       gray-50
--color-surface:       #FFFFFF
--color-border:        gray-200
--color-border-hover:  gray-300
--color-text-title:    gray-900
--color-text-body:     gray-700
--color-text-muted:    gray-500
--color-link:          navy-600
```

### 색 사용 원칙
- 화면의 대부분은 그레이(배경/텍스트/보더)로 구성한다.
- 네이비는 버튼·활성 상태·링크·선택된 행 등 **강조 지점에만 절제해서** 쓴다.
- 모서리 반경 8px, 그림자는 최소한으로(거의 안 씀), 보더로 영역을 구분한다.
- 전반적으로 Linear / Vercel 대시보드 같은 정돈되고 현대적인 B2B SaaS 톤.

---

## 2. 공통 레이아웃 (모든 페이지가 공유)

### 좌측 고정 사이드바
- 배경: navy-900, 텍스트: 흰색/연한 회색, 활성 메뉴는 navy-800 또는 navy-700 배경으로 강조
- 상단: FlareLane 로고
- 그 아래: 프로젝트 선택 영역 (드롭다운, 더미: "https://junyeongchoi.github.io/")
- 메뉴 구조 (그룹 라벨 포함):
  - (그룹 없음) 대시보드 / 고객 여정 자동화 / 반복 발송 메시지
  - **채널**: 푸시 알림 / 카카오 알림톡 / 카카오 브랜드메시지 / 문자 / 이메일 / 인앱 메시지 (팝업)
  - **대상**: 유저 / 기기
  - **설정**: 프로젝트 / 고객 데이터 연동 / 채널
- 하단: 사용자 아바타(한결), 언어 선택(한글)

### 메인 영역
- 배경: gray-50
- 상단에 페이지 제목 (예: "기기 및 발송 현황", "고객 데이터 연동")
- 콘텐츠는 흰색 카드(보더 gray-200, 모서리 8px) 단위로 구성

### 공통 컴포넌트 (반드시 재사용 가능하게 분리해서 만들 것)
- `<SectionCard>`: 제목 왼쪽에 navy-800 세로 막대 표시 + 카드 래퍼
- `<DataTable>`: TanStack Table 기반. 정렬/필터/페이지네이션/행 선택 지원
- `<StatusBadge>`: 구독/미구독, 활성화 등 상태 배지
- `<MetricCard>`: 아이콘 + 라벨 + 큰 숫자
- 버튼, 입력창, 드롭다운은 shadcn 기본 컴포넌트에 토큰만 적용

> 이 공통 컴포넌트 재사용이 이 데모의 핵심 메시지입니다.
> ("컴포넌트를 통일하면 기능 추가가 N배 → 1배")

---

## 3. 페이지별 명세 (5개)

### 페이지 1 — 대시보드 (`/dashboard`)
기존 구조 그대로, 위에서부터 세로로 쌓는다.

1. **기기 및 발송 현황**: 좌우 2개 카드. 왼쪽 "웹 푸시", 오른쪽 "앱 푸시".
   - 웹 푸시 — 전체 기기 수(데스크탑 395 / 모바일 23), 알림 구독 기기 수(데스크탑 48 / 모바일 9)
   - 앱 푸시 — 전체 기기 수(안드로이드 253 / iOS 9), 알림 구독 기기 수(안드로이드 2 / iOS 0)
   - 각 숫자 옆 작은 아이콘(데스크탑/모바일/안드로이드/iOS)
2. **신규 등록 기기**: 라인+영역 차트. x축 날짜(5/02~5/29), 선/면적 navy-800. 우측 상단 "CSV로 내보내기".
3. **발송 & 클릭**: 이중 y축 라인 차트. '발송'(navy-800), '클릭'(앰버 계열). 범례 + "CSV로 내보내기".
4. **발송 시간대별 평균 클릭률**: 막대 차트. x축 0~23시, y축 0~100%. 막대 navy-800. "CSV로 내보내기".
5. **가장 클릭률 높은 메시지**: 좌우 2개 카드(콘솔 / API). 컬럼: 클릭률, 메시지, 클릭 합계. 1위 행 순위 배지. 빈 상태 "데이터가 없습니다". 각 카드 "더 보기".
6. **최근 이벤트**: 접을 수 있는 행 하나. 우측 펼치기 화살표.

### 페이지 2 — 고객 데이터 연동 (`/data-management`)
1. **최근 이벤트**: 카드 안 데이터 테이블.
   - 헤더 우측 필터 줄: 드롭다운(기본 "유저 ID"), ID 입력창, "Event Type" 입력창, 날짜 범위(2026-05-25 ~ 2026-06-01), 새로고침 아이콘 버튼
   - 우측에 "CSV로 내보내기"
   - 컬럼: 생성 시간, 타입, 대상, 대상 ID, 데이터
   - '타입' 값(회색 코드 스타일 배지): @iam_displayed, deferred, @first_session, @iam_closed
   - '데이터' 컬럼은 긴 JSON 문자열(말줄임 처리), 각 행 우측 끝 복사 아이콘 버튼
   - 세로 스크롤(약 6행 고정), 더미 8행
   - **더미 ID/JSON 값은 실제 데이터가 아닌 가짜 값으로 채울 것**
2. **이벤트 내보내기 (상태 바)**: 한 줄 카드. 구분선으로 구획.
   - "이벤트 내보내기" + 초록 점 "활성화" 배지 / "최근 내보내기 2026.05.29 10:31:23" / "내보내는 이벤트 개수 9" / 우측 "설정" 버튼
3. **이벤트 / 태그 (좌우 2단)**: 동일 구조 관리 테이블.
   - 각 카드 헤더: 제목 + "숨긴 항목 보기"(눈 아이콘) + navy-800 "추가" 버튼
   - 컬럼: 이름, 설명, (우측 끝 ⋯ 더보기 메뉴)
   - 이벤트 더미: @first_session("(기본) 플레어레인에 기기가 처음 등록되었을 때") 외 2~3개
   - 태그 더미: "가"·"나"("(데이터 연동되어 자동 추가됨)"), "콩"("콩자반") 등
   - '이름' 값은 navy-600 링크 색

### 페이지 3 — 전체 유저 (`/all-users`)
- 페이지 제목 "전체 유저", 우측 상단 User ID 검색창 + "검색" 버튼(navy-800)
- 데이터 테이블 (TanStack Table)
  - 컬럼: 유저 ID, 이름, 전화번호, 마지막 접속 시간, 태그, (우측 끝 "상세 정보" 버튼)
  - 정렬(헤더 클릭), 페이지네이션, 검색
  - 더미 행 12개 정도 (유저 ID 예: temp_1, temp_2, parkjh- 등. 이름·전화번호는 "-"로 비워도 됨)
  - 마지막 접속 시간은 "4일 전", "12일 전" 같은 상대 표기
  - 태그 컬럼엔 작은 "태그" 배지

### 페이지 4 — 새 메시지 (`/push/new-message`)
3단계 폼 레이아웃(번호 매긴 스텝), 우측에 미리보기 패널.
1. **캠페인 명**: 입력창
2. **대상**: 탭(기기 세그먼트 / 유저 ID / 기기 / 파일 업로드). "포함" 세그먼트 선택 드롭다운, "제외" 접이식 영역, "추정 기기 수: 0"
3. **메시지**: 우측 상단 버튼들(AI / 최근 발송 메시지 / 템플릿 선택). 제목 입력창, 내용 입력창, URL 입력창(각 우측에 `{}` 변수 버튼), 이미지 업로드 영역
- 우측 미리보기 패널: 앱 푸시(Android) / 웹 푸시 토글, 휴대폰 목업 안에 알림 미리보기

### 페이지 5 — 프로젝트 설정 (`/project`)
1. **프로젝트 카드**: 우측 상단 "ENTERPRISE" 배지 + "고급 설정" 링크
   - 프로젝트명("https://junyeongchoi.github.io/"), 대체 시간대("(GMT+09:00) Asia/Seoul")
   - 프로젝트 ID(더미 UUID, 복사 버튼), APIKEY(마스킹 표시 ••••, 복사 버튼)
   - 우측 하단 navy-800 "수정" 버튼
2. **구매 전환 이벤트 카드**: 라디오(미설정 / 설정). 설정 시 표(이벤트/금액 데이터/수량 데이터/통화/기여 기간) — 더미: event / price / quantity / KRW / 6일. 우측 하단 "수정" 버튼

---

## 4. 작업 순서 권장

1. 프로젝트 세팅 + 토큰 등록 + 공통 레이아웃(사이드바)부터 완성한다.
2. 공통 컴포넌트(SectionCard, DataTable, MetricCard, StatusBadge)를 먼저 만든다.
3. 페이지는 대시보드 → 전체 유저 → 고객 데이터 연동 → 새 메시지 → 프로젝트 순으로 만든다.
4. 각 페이지는 위 공통 컴포넌트를 **재사용**해서 구성한다. 페이지마다 테이블을 새로 짜지 않는다.

## 5. 완료 기준
- `npm run dev`로 5개 페이지가 사이드바 내비게이션으로 이동 가능
- 모든 화면이 동일한 네이비/그레이 토큰과 공통 컴포넌트를 사용
- 더미 데이터로 채워져 있고, 정렬·검색·페이지네이션 등 기본 인터랙션이 동작
- 실제 API·인증·외부 연동 없음
